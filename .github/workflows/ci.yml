name: Python CI with Fake CD

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Syntax check (main.py, config.py)
        run: |
          python -m py_compile main.py
          python -m py_compile config.py

      - name: Fake deploy
        run: |
          echo "🚀 Fake deployment started..."
          sleep 2
          echo "🤖 Telegram bot deployed successfully (fake)"
